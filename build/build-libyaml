#!/bin/bash
PATH="${PATH}:/opt/miyoo/bin:/opt/miyoo/arm-miyoo-linux-uclibcgnueabi/sysroot/usr/bin"
CROSS_COMPILE=/opt/miyoo/bin/arm-miyoo-linux-uclibcgnueabi-
CC=${CROSS_COMPILE}gcc
AR=${CROSS_COMPILE}ar
AS=${CROSS_COMPILE}as
LD=${CROSS_COMPILE}ld
CXX=${CROSS_COMPILE}g++
HOST=arm-miyoo-linux-uclibcgnueabi
PREFIX=/opt/miyoo/

git clone --depth=1 --branch 0.2.5 https://github.com/yaml/libyaml
cd libyaml
./bootstrap
./configure --host=${HOST} --prefix=${PREFIX}
make install

# apt-get install -y wget
# wget https://www.libsdl.org/release/SDL2-2.0.14.tar.gz && \
#   tar xzf SDL2-2.0.14.tar.gz && \
#   cd SDL2-2.0.14 && \
#   ./configure --host=${HOST} --prefix=${PREFIX} \
#       --enable-video-directfb=yes \
#       --enable-video-opengl=no \
#       && \
#   make && \
#   make install
#
# wget https://www.libsdl.org/projects/SDL_ttf/release/SDL2_ttf-2.0.15.tar.gz && \
#   tar xzf SDL2_ttf-2.0.15.tar.gz && \
#   cd SDL2_ttf-2.0.15 && \
#   ./configure --host=${HOST} --prefix=${PREFIX} --disable-video-opengl \
#   make && \
#   make install
